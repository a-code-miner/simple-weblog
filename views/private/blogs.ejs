<% if (blogs.length > 0) { %>
<h2>آخرین پست‌ها</h2>
            <div class="table-responsive">
                <table class="table table-striped table-sm">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>عنوان</th>
                            <th>متن</th>
                            <th>تاریخ</th>
                            <th>وضعیت</th>
                            <th>تست</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (let blog of blogs) { %>
                        <tr>
                            <td><%= blog._id %></td>
                            <td><a href="/blog/<%= blog._id %>"></a><%= blog.title %></td>
                            <td><%= blog.body %></td>
                            <td><%= formatDate(blog.created_at) %></td>
                            <td><span class="badge <%= blog.status === 'عمومی' ? 'badge-success' : 'badge-info' %>"><%= blog.status %></span></td>
                            <td>تست</td>
                        </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </main>
    </div>
</div>
    <% } else { %>
        <div class="text-center">
            <h3 class="mt-2">در حال حاضر هیچ پستی توسط شما در پایگاه داده ثبت نشده است.</h3>
        </div>
        <% } %>
